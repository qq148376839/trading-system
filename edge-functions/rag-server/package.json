{
  "name": "rag-server",
  "version": "1.0.0",
  "private": true,
  "description": "RAG MCP Server on Cloudflare Workers — Vectorize + Workers AI + KV",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format json",
    "cf-typegen": "wrangler types",
    "index": "tsx scripts/index-project.ts",
    "index:full": "tsx scripts/index-project.ts --full",
    "index:file": "tsx scripts/index-project.ts --file"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.26.0",
    "agents": "^0.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
