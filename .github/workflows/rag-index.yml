name: RAG Index

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'api/src/services/**'
      - 'api/src/routes/**'
      - 'api/src/utils/**'
      - 'api/src/config/**'
      - 'api/src/middleware/**'
      - 'api/migrations/**'
      - 'CLAUDE.md'
      - 'PROJECT_STATUS.md'
      - 'CHANGELOG.md'
      - 'CODE_MAP.md'
      - '.claude/agents/**'
      - '.claude/teams/**'
  workflow_dispatch: # 手动触发

jobs:
  index:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install tsx
        run: npm install -g tsx

      - name: Restore index manifest
        uses: actions/cache@v4
        with:
          path: edge-functions/rag-server/.index-manifest.json
          key: rag-manifest-${{ github.sha }}
          restore-keys: rag-manifest-

      - name: Run incremental index
        working-directory: edge-functions/rag-server
        env:
          RAG_API_TOKEN: ${{ secrets.RAG_API_TOKEN }}
          RAG_URL: https://rag.riowang.win
        run: tsx scripts/index-project.ts
