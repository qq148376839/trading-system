# Frontend 服务开发环境 Dockerfile
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制 package.json 和 package-lock.json（如果存在）
COPY package*.json ./

# 安装所有依赖
RUN npm ci

# 暴露端口
EXPOSE 3000

# 开发模式，支持热重载
CMD ["npm", "run", "dev"]

